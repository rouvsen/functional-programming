package org.example.functional_interfaces;

public class UnaryOperator_FInterface {
    // <T> type-inda alir, ele <T> type-inda da return edir
    // Integer alir, Integer return edir,
    // f.e: Integer alir square-ini tapir, integer olaraq da return edir

    //Function-i extends edir, Function <T> goturur, <R> return edir
    //Function<T><R>

    //Bu ise Function-i, Function<T><T> olaraq istifade edir
}
